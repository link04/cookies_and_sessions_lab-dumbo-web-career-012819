
<ul>
  <li><%= session[:cart] %> </li>
</ul>


<%= form_for add_products_path do |f| %>
  <%= f.label :product %>:
  <%= f.text_field :product, id: 'product' %>
  <%= f.submit 'add to cart' %>
<% end %>